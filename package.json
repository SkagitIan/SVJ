{
  "name": "skagitvalleyjobs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute skagitjobs --file=schema.sql",
    "db:init:remote": "wrangler d1 execute skagitjobs --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.5"
  }
}
